# Signal Configuration
# Phone number in E.164 format (country code + number, no spaces)
SIGNAL_PHONE_NUMBER=+12025551234

# Anthropic API
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Google Calendar
# Path to service account JSON key file
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=/opt/family-coordinator/service-account-key.json
# Calendar ID (find in Calendar Settings -> Calendar ID)
GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com
# Family timezone (IANA format)
FAMILY_TIMEZONE=Europe/Berlin

# Database
# PostgreSQL connection string
# Install PostgreSQL: sudo apt install postgresql postgresql-contrib
# Create database: sudo -u postgres createdb family_coordinator
# Create user: sudo -u postgres createuser -P family_coordinator
# Grant access: sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE family_coordinator TO family_coordinator;"
DATABASE_URL=postgresql://family_coordinator:your-db-password@localhost:5432/family_coordinator

# Health Check Port
HEALTH_PORT=3000

# Application
# Environment: production
NODE_ENV=production

# Log level: info (or warn/error for quieter logs)
LOG_LEVEL=info
