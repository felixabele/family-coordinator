---
phase: 06.1-fix-wrong-calender-day-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/calendar/timezone.test.ts
  - src/calendar/timezone.ts
autonomous: true
must_haves:
  truths:
    - "Friday events are displayed as Friday (Freitag), not Saturday (Samstag)"
    - "All 7 weekdays display correct German names"
    - "Weekend queries show correct day names for Saturday and Sunday events"
    - "Day-of-week mapping is accurate across all query types (heute, morgen, Wochenende, specific dates)"
  artifacts:
    - path: "src/calendar/timezone.test.ts"
      provides: "Comprehensive tests for formatDayName covering all 7 weekdays"
      min_lines: 60
      exports: ["describe formatDayName"]
    - path: "src/calendar/timezone.ts"
      provides: "Fixed formatDayName function with correct day-of-week mapping"
      contains: "formatDayName"
  key_links:
    - from: "src/calendar/timezone.test.ts"
      to: "src/calendar/timezone.ts"
      via: "import and test formatDayName"
      pattern: "import.*formatDayName.*timezone"
    - from: "src/calendar/timezone.ts formatDayName"
      to: "Luxon DateTime API"
      via: "DateTime.fromFormat with timezone"
      pattern: "DateTime\\.fromFormat.*zone.*timezone"
---

<objective>
Fix the incorrect day-of-week mapping bug where calendar events display with wrong weekday names (e.g., Friday events showing as Saturday).

Purpose: Users receive incorrect information about when events occur, potentially causing missed appointments. The bug is isolated to the `formatDayName()` function in `src/calendar/timezone.ts`, which uses Luxon to convert YYYY-MM-DD date strings to German weekday names.

Output: Fixed `formatDayName` function with comprehensive test coverage for all 7 weekdays, ensuring accurate day-of-week display across all query types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-fix-wrong-calender-day-mapping/06.1-CONTEXT.md
@.planning/phases/06.1-fix-wrong-calender-day-mapping/06.1-RESEARCH.md
@src/calendar/timezone.ts
@src/signal/listener.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive tests for formatDayName function</name>
  <files>src/calendar/timezone.test.ts</files>
  <action>
Create test file `src/calendar/timezone.test.ts` with comprehensive test coverage for the `formatDayName` function using Vitest and Luxon.

Test suite must include:

1. **All 7 weekdays test (primary diagnostic):**
   - Test dates: 2026-03-16 (Monday) through 2026-03-22 (Sunday)
   - Each test verifies correct German weekday name: Montag, Dienstag, Mittwoch, Donnerstag, Freitag, Samstag, Sonntag
   - Use actual calendar dates (verify 2026-03-20 is Friday, not Saturday)

2. **Weekend-specific tests:**
   - Saturday (2026-03-21) should return "Samstag"
   - Sunday (2026-03-22) should return "Sonntag"
   - Verify these match weekend query expectations

3. **DST boundary edge case:**
   - Test dates around DST transition (last Sunday of March in Europe/Berlin)
   - Date 2026-03-29 (Sunday when DST starts) should still return "Sonntag"
   - Date 2026-03-30 (Monday after DST) should return "Montag"

4. **Timezone consistency test:**
   - Same date string with Europe/Berlin timezone should always return same result
   - Verify formatDayName is deterministic

Use vitest's `describe`, `it`, `expect` pattern. Import `formatDayName` from `./timezone`. Use timezone constant `"Europe/Berlin"` matching production environment.

**Reference pattern from research:**

```typescript
import { describe, it, expect } from "vitest";
import { formatDayName } from "./timezone";

describe("formatDayName", () => {
  const tz = "Europe/Berlin";

  it("should format Monday correctly", () => {
    expect(formatDayName("2026-03-16", tz)).toBe("Montag");
  });
  // ... continue for all 7 days
});
```

This test will REVEAL the bug by showing which day mappings are incorrect.
</action>
<verify>
Run tests with `npm test src/calendar/timezone.test.ts` and observe results. Tests will likely FAIL initially (revealing the bug). Verify:

- Test file exists and imports formatDayName correctly
- All 7 weekday tests are present (Mon-Sun)
- Test output clearly shows expected vs actual day names
- Tests use dates from March 2026 (known weekday mappings)
  </verify>
  <done>
  Test file exists with comprehensive coverage for all 7 weekdays, weekend-specific tests, DST edge case, and timezone consistency. Test execution reveals which weekday mappings are incorrect, providing diagnostic data for the fix.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix formatDayName bug based on test failures</name>
  <files>src/calendar/timezone.ts</files>
  <action>
Analyze the test failures from Task 1 to identify the root cause of the day-of-week mapping bug. Based on research, likely causes are:

**Most likely (Pitfall 3 - Index mismatch):**

- Luxon's `weekday` property uses ISO 8601 (1=Monday, 7=Sunday)
- Check if there's any manual array mapping that's off by one
- Verify `.toFormat("cccc")` returns correct German names

**Also check (Pitfall 1 - Timezone parsing):**

- Verify `{ zone: timezone }` is correctly applied when parsing date-only strings
- Check if midnight boundary causes day shift in timezone conversion

**Debugging approach:**

1. Add temporary console.log in formatDayName to verify:
   - Input date string (e.g., "2026-03-20")
   - Parsed DateTime.weekday number (should be 5 for Friday)
   - Output from .toFormat("cccc") (should be "Freitag")

2. Once bug is identified, apply fix. Possible fixes:
   - If weekday number is correct but name is wrong → Locale issue, verify `.setLocale("de")` is applied
   - If weekday number is off by 1 → Timezone parsing issue, verify zone option
   - If format returns English → Locale not set, add `.setLocale("de")`

3. Remove debug logging after fix is confirmed

**Current implementation (lines 93-96):**

```typescript
export function formatDayName(date: string, timezone: string): string {
  const dt = DateTime.fromFormat(date, "yyyy-MM-dd", { zone: timezone });
  return dt.setLocale("de").toFormat("cccc");
}
```

This LOOKS correct, so the bug may be subtle. After debugging, update the implementation to fix the identified issue.

**Important:** Do NOT change the function signature or remove timezone handling. Keep using Luxon (already in use project-wide). Maintain German locale output.
</action>
<verify>
Run tests again with `npm test src/calendar/timezone.test.ts`. All tests must PASS:

- All 7 weekday tests pass (Montag through Sonntag)
- Weekend tests pass (Samstag, Sonntag)
- DST edge case passes
- No console warnings or errors

Also verify the fix doesn't break existing functionality:

- Run full test suite: `npm test` (currently no other tests, but verify it doesn't error)
- Check file passes type check: `npx tsc --noEmit`
  </verify>
  <done>
  All formatDayName tests pass, confirming correct day-of-week mapping for all 7 weekdays. Friday events now display as "Freitag" (not "Samstag"). The bug is fixed and has test coverage to prevent regression.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Verify fix with weekend query scenario</name>
  <files>src/calendar/timezone.test.ts</files>
  <action>
Add an integration-style test to `src/calendar/timezone.test.ts` that simulates the exact user scenario from the bug report:

**Scenario:** User has a Friday event and asks "what do we do this weekend"

The test should:

1. Create a test that verifies formatDayName behavior for the weekend query flow
2. Test Friday (day before weekend) returns "Freitag" (not "Samstag")
3. Test Saturday returns "Samstag"
4. Test Sunday returns "Sonntag"

This is a regression test ensuring the specific reported bug (Friday → Saturday) is fixed.

Add a new describe block:

```typescript
describe("formatDayName - weekend query scenario", () => {
  const tz = "Europe/Berlin";

  it("should correctly identify Friday before weekend", () => {
    expect(formatDayName("2026-03-20", tz)).toBe("Freitag");
  });

  it("should correctly identify Saturday in weekend", () => {
    expect(formatDayName("2026-03-21", tz)).toBe("Samstag");
  });

  it("should correctly identify Sunday in weekend", () => {
    expect(formatDayName("2026-03-22", tz)).toBe("Sonntag");
  });
});
```

This test documents the specific bug that was reported and ensures it stays fixed.
</action>
<verify>
Run tests with `npm test src/calendar/timezone.test.ts`. All weekend scenario tests must pass. Verify:

- Friday is NOT misidentified as Saturday
- Saturday and Sunday are correctly identified
- Test names clearly document the bug scenario
  </verify>
  <done>
  Weekend query scenario tests pass, confirming the specific reported bug (Friday showing as Saturday) is fixed. Test suite now includes both comprehensive weekday coverage and the specific regression test for the reported issue.
  </done>
  </task>

</tasks>

<verification>
**Automated verification:**
- All tests pass: `npm test src/calendar/timezone.test.ts`
- Type check passes: `npx tsc --noEmit`
- Code formatting: `npx prettier --check src/calendar/timezone.ts src/calendar/timezone.test.ts`

**Manual verification (if bot is running):**

1. Send message: "was machen wir am Wochenende"
2. Verify response shows correct day names for any Friday/Saturday/Sunday events
3. Check that Friday events say "Freitag" (not "Samstag")

**Regression check:**

- Weekend queries (fixed in quick tasks #1, #2) still work correctly
- Date range queries still show accurate day names
- All-day events still display correctly
  </verification>

<success_criteria>

- Test file exists with 10+ test cases covering all weekdays
- All tests pass (100% success rate)
- formatDayName function correctly maps all 7 weekdays to German names
- Specific bug (Friday → Saturday) is fixed and has regression test
- No TypeScript errors or formatting issues
- Code is committed with descriptive message referencing bug fix
  </success_criteria>

<output>
After completion, create `.planning/phases/06.1-fix-wrong-calender-day-mapping/06.1-01-SUMMARY.md`
</output>
